1. Create operations.js exporting:

    square(n)

    cube(n)

    factorial(n)

Import only square and cube in main.js and display results.

2. Export a constant PI = 3.14 from constants.js.
   Import it and calculate the area of a circle.

3. Create number.js with:

    isEven(n)

    isOdd(n)
    Export and test both functions.